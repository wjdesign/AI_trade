# AI_trade 專案脈絡與理解交接

> 這份文件封裝目前 Claude 對這個專案的完整理解，給接手的 Claude 或開發者使用。讀完這份不用再做 audit 也能掌握全貌。

---

## 一句話定位

這是一套**台股 AI 自動交易機器人**，基於永豐金 Shioaji API，目前運行於**模擬交易環境**，主打盤中零股短線輪動策略，並透過 Telegram 即時推播訊號與新聞摘要。

---

## 技術棧

| 層級 | 元件 |
|------|------|
| 語言 / 版本 | Python 3.12+ |
| 套件管理 | `uv`（推薦）或 `pip` |
| 券商 API | 永豐金 Shioaji（`shioaji` 套件） |
| 資料來源 | Shioaji（即時報價）、yfinance（回測）、TWSE Open Data（三大法人籌碼） |
| 新聞聚合 | 鉅亨網 JSON API、Yahoo 股市 RSS、Google News RSS |
| AI 分析 | OpenAI GPT-4o（情緒評分，可選用） |
| 通知 | Telegram Bot API |
| 部署 | GitHub Actions（公開 repo，免費） |

---

## 投資策略哲學（3 個核心信念）

把這個 bot 擬人化，它是一個**短線、保守、靠分散和早出場活著**的當沖傾向小資族。三個核心信念：

### 1. 它不預測方向，只跟風

整套策略沒有任何「我覺得這檔會漲」的判斷，全部都是「市場已經在動了，我跟上去」：

- VWAP 突破 → 盤中買盤已經贏了賣盤
- RSI < 70 → 確認動能但還沒過熱
- RVOL ≥ 1.5 → 量能確認放大
- 法人籌碼 ≥ -0.3 → 大戶站在我這邊
- 現價 > MA50 → 長期趨勢站對邊

它是個**順勢者**，不是先知。

### 2. 它假設自己會錯

所有的進場條件都很嚴格（要同時通過 7、8 道過濾），但所有的出場條件都很寬鬆（任何一個觸發就跑）。這是典型的「**入場慢、出場快**」非對稱設計。

### 3. 它知道自己的劣勢是手續費

46,000 元預算、零股交易、每筆最低 11,000 元下單——這個尺寸對散戶很合理，但對策略本身是個重大束縛。所以才有 `MIN_ORDER_VALUE` 這種「寧可不做也不要被手續費吃掉」的設計。

---

## 雙引擎策略框架

`StrategyAllocator` 根據 **0050 近 20 日年化波動率** 動態切換主策略：

| 市場狀態 | 判斷條件 | 動能策略佔比 | 均值回歸佔比 |
|---------|---------|------------|-----------|
| TRENDING（趨勢市） | 年化波動率 < 18% | 80% | 20% |
| RANGING（盤整市） | 年化波動率 ≥ 18% | 30% | 70% |

> ⚠️ **重要**：原始程式碼的閾值是 `0.015`（1.5%，測試用值），台股實際波動率幾乎都在 10~25%，意味著策略**永遠判定為盤整市**。已在這次 audit 修正為 `0.18`。

### 動能策略

「**追強勢股短打**」：

```
進場條件（須全部通過）：
  ├─ 未持有此股
  ├─ 滑點 OK（買賣價差 ≤ 1%）
  ├─ 現價 > MA50（長期趨勢向上）
  ├─ ATR / 股價 ≤ 3%（排除跳空缺口風險）
  ├─ RSI < 70（避免追高；趨勢市中動態放寬至 75）
  ├─ 0 < VWAP 乖離 ≤ 3%（日內強勢但未過熱）
  ├─ RVOL ≥ 1.5（量能確認放大）
  ├─ 法人態度 ≥ -0.3（三大法人未大幅賣超）
  └─ 下單金額 ≥ 11,000 元（避免手續費侵蝕）
```

### 均值回歸策略

「**抓恐慌反彈**」：

```
進場條件：
  ├─ RSI < 30（超賣）
  ├─ 現價 < VWAP（盤中偏弱）
  └─ 法人態度 ≥ -0.5（法人沒大幅賣超）
```

---

## 風控設計（四層停損）

出場邏輯是整個專案最用心的地方，分四層解決不同問題。**每輪必跑、不受大盤過濾影響**：

### A. ATR 動態止損（防止單筆崩盤）

```python
止損價 = min(max(進場價 - 1.5×ATR, 進場價 × 97%), 進場價 × 97.5%)
```

這個三明治公式很巧妙：
- ATR 太大時用 3% 上限（避免被波動性大的股票拖太深）
- ATR 太小時用 2.5% 下限（避免被微小雜訊假止損誤殺）

### B. 成本保衛（防止獲利變虧損）

獲利達 2% 後，止損線自動上移到進場成本。心理上叫「至少不會輸」，數學上是把預期值從負偏拉成正偏。

### C. 動態移動止盈（讓利潤跑）

獲利達 1.5% 後，從歷史高點回吐 `max(0.6×ATR, 1.5%)` 就跑。波動越大、容忍回吐越大；波動越小、跑得越快。

### D. 時間停損（防止溫水煮青蛙）

⚠️ **這裡有重大設計分歧**：

- **README 描述**：30 分鐘日內停損（進場後 30 分鐘還在成本 ±0.5% 內就跑）→ 當沖風格
- **程式碼實際**：5 工作天波段停損（持有 5 工作天還沒漲到 1.5% 就跑）→ 波段風格

這是 Wayne 待決定的策略方向（HANDOFF.md 中的決策 1）。

---

## 監控標的概況

| 類別 | 數量 | 來源 |
|------|------|------|
| 回測驗證過的精選（README 描述的核心） | 12 檔 | yfinance 2021-2026 回測，PF ≥ 1.1、Sharpe ≥ 0.6 |
| 圖片辨識自選清單（未經回測） | 60+ 檔 | 註解明說「待回測驗證」 |
| 長期持有不停損（`LONG_TERM_HOLD`） | 1 檔（0050） | 不納入停損/停利監控 |
| 漏斗掃描器動態加入 | 最多 5 檔/日 | 每日 09:20 觸發 `FunnelScanner` |

> ⚠️ 60+ 檔未驗證股直接混進每分鐘掃描，是這次 audit 發現的問題之一。是否要瘦身為 Wayne 待決定（HANDOFF.md 中的決策 2）。

---

## 系統運作流程

### 啟動流程

```
程式啟動
  ├─ 訂閱 12 檔股票的即時買賣報價（BidAsk），快取供滑點判斷
  ├─ 查詢帳戶餘額（模擬帳戶回傳 0 時沿用設定值 46,000 元）
  ├─ 同步已有持倉（重啟後不遺漏部位）
  └─ Telegram 推播：系統設定 + 目前持倉 + 啟動情緒分析 + 最新新聞
```

### 交易時間主循環（09:05–13:25，每 60 秒一輪）

```
每輪固定執行：
  ├─ Step 1  出場監控（最優先，不受任何過濾影響）
  │            逐一檢查每個持倉 → 觸發止損/止盈立即賣出
  │            ※ 零股 T+1：當日買進的部位當日不賣
  │
  ├─ Step 2  大盤過濾
  │            0050 現價 > MA20 (含 0.1% 遲滯帶) → 繼續
  │            0050 現價 < MA20 → 跳過本輪進場掃描
  │
  ├─ Step 3  情緒評分（可關閉）
  │            SENTIMENT_ENABLED=False（預設）→ 固定分數 1.0
  │
  ├─ Step 4  市場狀態判斷
  │            0050 近 20 日年化波動率 < 18% → 趨勢市
  │            0050 近 20 日年化波動率 ≥ 18% → 盤整市
  │
  └─ Step 5  掃描 70+ 檔股票，找進場候選
               全部評估完 → 依綜合分排序 → 高分優先買進 → 最多 N 個部位（依 MAX_POSITIONS 退化值）
```

### 漏斗掃描（每日 09:20 觸發一次）

```
FunnelScanner 三層漏斗：
  ├─ 找符合條件的當日強勢股
  ├─ 最多取 FUNNEL_MAX_RESULTS = 5 檔
  └─ 加入當日 watch_list，與固定監控股一起被每分鐘掃描
```

### 非交易時間

```
每 30 分鐘推播今日最新新聞摘要至 Telegram
```

---

## 關鍵程式碼結構

```
AI_trade/
├── bot.py                    # 主交易機器人（121KB，含所有參數和主迴圈）
├── backtest.py               # 日 K 回測引擎（yfinance / Shioaji 雙模式）
├── minute_backtest.py        # 分鐘 K 回測引擎（Shioaji，需模擬帳戶）
├── main.py                   # 連線測試 & API 測試狀態查詢
├── check_account.py          # 正式帳戶餘額查詢
├── optimize.py               # 參數最佳化（grid search）
├── src/ai_trade/
│   ├── __init__.py
│   ├── client.py             # ShioajiClient 封裝類別
│   ├── news.py               # 新聞聚合器（鉅亨網 / Yahoo / Google News）
│   ├── scanner.py            # 三層漏斗掃描器（README 說「已不使用」是錯的）
│   ├── strategy.py           # 多策略框架（StrategyAllocator）
│   └── chips.py              # 籌碼流向分析（智能日期回溯）
├── pyproject.toml            # 套件設定（uv 管理）
├── requirements.txt          # pip 用備援
├── uv.lock                   # 鎖定版本
├── .env.example              # 環境變數範本
├── Sinopac.pfx               # 永豐金憑證檔（不要 commit！）
├── .github/workflows/
│   └── trading_bot.yml       # GitHub Actions 自動部署
├── AUDIT_REPORT.md           # 本次 audit 報告（新增）
├── GITHUB_ACTIONS_SETUP.md   # GitHub Actions 部署 SOP（新增）
├── HANDOFF.md                # 待辦交接文件（新增）
└── PROJECT_CONTEXT.md        # 本檔案（新增）
```

---

## 重要參數速查表

> 這些是**當前程式碼實際值**，不是 README 描述值。

| 參數 | 實際值 | 用途 |
|------|--------|------|
| `INITIAL_CAPITAL` | 68000 | 原始投入資金（用於累計損益計算） |
| `TOTAL_BUDGET` | 46000 | 總可用預算（元） |
| `MAX_POSITIONS` | 7（實際退化 4） | 最多同時持有部位數 |
| `POSITION_SIZE` | 11500 | 單筆下單金額（動態計算） |
| `MIN_ORDER_VALUE` | 11000 | 最低下單金額（防手續費侵蝕） |
| `STOP_LOSS_PCT` | 0.03（3%） | 強制止損 |
| `TRAILING_START` | 0.015（1.5%） | 移動止盈啟動點 |
| `TRAILING_PULLBACK` | 0.015（1.5%） | 移動止盈固定回撤 |
| `TRAILING_ATR_MULT` | 0.6 | 動態回撤倍數 |
| `BREAKEVEN_TRIGGER` | 0.02（2%） | 成本保衛啟動點 |
| `TIME_STOP_BDAYS` | 5 | 時間停損天數 |
| `SLIPPAGE_LIMIT` | 0.01（1%） | 滑點上限 |
| `ATR_MAX_PCT` | 0.03（3%） | ATR 過熱保護 |
| `MA_TREND_PERIOD` | 50 | 個股趨勢過濾均線 |
| `RVOL_MIN` | 1.5 | 相對成交量門檻 |
| `RSI_OVERBOUGHT` | 70 | RSI 超買門檻 |
| `RSI_OVERBOUGHT_LAX` | 75 | RSI 放寬上限（趨勢市） |
| `VWAP_MAX_GAP` | 0.03（3%） | VWAP 乖離率上限 |
| `MARKET_INDEX` | "0050" | 大盤指數代碼 |
| `SCAN_INTERVAL` | 60 秒 | 主循環間隔 |
| `_VOL_THRESHOLD` (strategy.py) | 0.18 | 雙引擎策略切換閾值（這次 audit 修正） |
| `SIMULATION` (env var) | true（預設） | 模擬/正式交易切換 |
| `CONFIRM_REAL_MONEY` (env var) | (未設) | 正式交易雙重防呆字串 |

---

## 設計優點

這個專案值得肯定的地方：

1. **風控優先**：四層停損設計很細膩，每層解決不同問題
2. **市場狀態切換**：雙引擎策略架構正確（雖然閾值參數寫錯導致從未真的雙向切換過）
3. **法人資料智能回溯**：`chips.py` 處理盤中/盤後/假日的日期邏輯正確
4. **MA20 遲滯帶**：避免在 MA20 附近震盪時每分鐘翻轉判斷
5. **Telegram 通知設計**：策略配置變更才推播、買賣成交即時推播、非交易時間每 30 分鐘新聞摘要，控制了通知噪音
6. **GitHub Actions workflow**：完整的雲端部署設計、Secrets 安全處理、Artifacts 備份還原（這次 audit 新加）
7. **雙重防呆機制**：要切正式交易必須**同時**設兩個 Secret 才會啟動，單一失誤無法觸發（這次 audit 新加）

---

## 設計弱點

需要 Wayne 注意的潛在問題：

1. **過度依賴 0050 做大盤判斷** — 當金融股或傳產走強時，0050 不一定反映得出來
2. **法人籌碼會延遲一天** — 盤中決策用的是昨日法人資料，快速反轉的市場會晚一步
3. **沒有部位輪動或加減碼邏輯** — 買進後只會等出場，不會「這檔表現比另一檔好，把錢挪過去」
4. **MAX_POSITIONS=7 名存實亡** — 因 MIN_ORDER_VALUE 限制實際只能 4 部位
5. **bot.py 過大（121KB）** — 所有參數、邏輯、訊號處理都在單檔，維護困難
6. **PINNED_STOCKS 60+ 檔未經回測** — 圖片辨識自選股直接放進掃描池
7. **TIME_STOP 設計分歧** — README 寫日內 30 分鐘、程式碼是 5 工作天，行為差異 100 倍以上

---

## Wayne 的脈絡

### 角色
- 個人散戶投資者
- 對程式交易感興趣，但**還沒完成永豐金開戶 + Shioaji API 申請**（這是時間瓶頸，1-2 週）
- 偏好「設好就忘」的雲端自動執行（已選擇 GitHub Actions 公開 repo）

### 已做的決策（本次對話）
- ✅ 不換元大證券（看過評估後決定繼續用永豐金 Shioaji）
- ✅ 線上申請永豐金證券帳號
- ✅ Fork repo 到 `wjdesign/AI_trade`（自己的 GitHub）
- ✅ 重新 clone 一份乾淨的 `D:\projects\wayne\AI_trade`（從原作者 yinyaoqing 的 fork 過來）
- ✅ 加上 `SIMULATION` 環境變數（避免改原始碼切換模式）
- ✅ 加上 `CONFIRM_REAL_MONEY` 雙重防呆
- ✅ 加上 Artifacts 自動備份（解決狀態無法持久問題）
- ✅ 公開 repo（不收費）
- ✅ 接受 cron 延遲（早盤 5-40 分鐘訊號可能會錯過）

### 待做的決策（HANDOFF.md 詳述）
- ⏸️ TIME_STOP 機制方向（5 工作天波段 vs 30 分鐘日內 vs 兩者並用）
- ⏸️ PINNED_STOCKS 60+ 檔未驗證股處理（只留 12 vs 抽到候選池 vs 全部交給 bot）
- ⏸️ MAX_POSITIONS 處理（改 4 vs 保 7 + 文件說明 vs 改 4 + MIN_ORDER_VALUE 回 9000）

### Wayne 的風格觀察
- 喜歡誠實、不灌水的評估
- 偏好「先了解再決定」（拒絕在不清楚的情況下做大決定）
- 願意把複雜的工作交給 Claude 處理，但保留策略決策權
- 安全意識高（會問安全性對比、會要求防呆）

---

## 接手 Claude 的注意事項

1. **這個專案不是你的，是 Wayne 的** — 不要私自改動商業邏輯，只動文件 / 註解 / 抽離 config / 修明顯 bug
2. **bot.py 是個 121KB 的大檔** — 動它時用 Read 局部 + Edit 局部，避免大規模重寫
3. **Wayne 還沒有 API 金鑰，無法實際跑 bot 驗證** — 你的所有改動需要靠靜態 audit + 單元測試驗證
4. **GitHub Actions 是公開 repo** — 任何改動都要 commit 之前先 push 給 Wayne review，不要私自推 main
5. **遇到不確定的策略行為改動，先停下來問 Wayne** — 不要自作主張
6. **回應請用繁體中文** — Wayne 是台灣用戶，所有對話都用繁體中文

---

## 對話歷史摘要

按時間順序，Wayne 在這次 Cowork 對話中問過的問題與我的回應：

1. **「這是什麼專案？」** → 我給了專案綜述（台股 AI 模擬交易機器人）
2. **「能改成元大版本嗎？」** → 我做了元大 vs 永豐 API 對比，建議不換
3. **「我先去申請永豐金證券帳號」** → 我提醒了 API 權限、模擬戶、CA 憑證流程
4. **「分析投資策略」** → 我做了完整策略哲學分析（即本文件主要內容）
5. **「3 件事都幫我處理」**（差異報告、修 bug、查衝突）→ 產出 AUDIT_REPORT.md，修了 2 個關鍵 bug
6. **「能 fork 嗎？」** → 引導他 fork 並重 clone
7. **「能直接在 GitHub 跑嗎？」** → 解釋 GitHub Actions workflow，產出 GITHUB_ACTIONS_SETUP.md
8. **「真實資金交易要從哪改？有環境變數嗎？」** → 加上 SIMULATION 環境變數機制
9. **「加防呆」** → 加上 CONFIRM_REAL_MONEY 雙重防呆，17 項測試全綠
10. **「狀態持久化能解決嗎？」** → 加上 Artifacts 備份還原
11. **「本機 vs GitHub Actions 哪個安全？」** → 做了完整對比，他選擇繼續 GitHub Actions
12. **「立即/短期/中期處理都幫我做」** → 我列出 11 項待辦，問 3 個策略決策（被 UI 中斷）
13. **「處理到哪了？」** → 報告進度
14. **「幫我輸出待處理事項，要轉到 Code」** → 產出 HANDOFF.md 和本檔案

---

## 重要：給 Code 接手 Claude 的明確指示

**第一步**：請先讀完這三個檔案：
1. `D:\projects\wayne\AI_trade\PROJECT_CONTEXT.md`（本檔案，專案脈絡）
2. `D:\projects\wayne\AI_trade\AUDIT_REPORT.md`（已做的 audit 與修正）
3. `D:\projects\wayne\AI_trade\HANDOFF.md`（待辦事項與決策）

**第二步**：先讓 Wayne 在 HANDOFF.md 的「3 個策略決策」各選一個答案。

**第三步**：按 HANDOFF.md 的「建議的執行順序」進行：
1. 先 commit baseline
2. 依決策動工
3. 每完成 2-3 個任務就 commit + push
4. 最後手動觸發 workflow 做端到端驗證

**第四步**：完成所有待辦後，請 Wayne 把 PROJECT_CONTEXT.md 和 HANDOFF.md 內容對應更新（決策已做、待辦已完成）。
